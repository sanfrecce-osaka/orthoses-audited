# frozen_string_literal: true

RSpec.describe Orthoses::Audited do
  it "has a version number" do
    expect(Orthoses::Audited::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
